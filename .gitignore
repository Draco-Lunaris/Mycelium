node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store
coverage/
demo-bundle/
# runtime data — keep the project folder a clean checkout. Root-anchored so
# they only match the repo-root runtime dirs, NOT the source packages
# (packages/core/src/shelves, packages/core/src/library).
/library/
/shelves/
**/.traces/
# Rendered Caddy gateway config — contains generated secrets, keep local.
# The template (deploy/Caddyfile.template) IS committed; this rendered file is not.
deploy/Caddyfile
# Playwright MCP browser-session artifacts (snapshots/console logs), local only.
.playwright-mcp/
