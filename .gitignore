node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store
coverage/
demo-bundle/
# runtime data — keep the project folder a clean checkout (named volumes in
# docker-compose.yml; these should never appear, but ignore as a backstop)
library/
shelves/
**/.traces/
